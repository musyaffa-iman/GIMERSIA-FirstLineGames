[gd_scene load_steps=4 format=3 uid="uid://df4ei1xo1axp0"]

[ext_resource type="Texture2D" uid="uid://brvkoudyg5mon" path="res://raddief/Sprites/Projectile/arrow.png" id="1_tex"]

[sub_resource type="GDScript" id="GDScript_nyyeu"]
script/source = "extends Area2D

@export var speed: float = 600.0
@export var lifetime: float = 1.5
var velocity: Vector2 = Vector2.ZERO
var damage: float = 15.0

func _ready():
	add_to_group(\"player_attack\")
	# Default layers/masks (1) so it hits default enemy hitboxes unless customized.
	# Auto-destroy after lifetime
	await get_tree().create_timer(lifetime).timeout
	queue_free()

func _physics_process(delta: float) -> void:
	position += velocity * delta

func set_velocity(v: Vector2) -> void:
	velocity = v

func set_damage(dmg: float) -> void:
	damage = dmg

func configure(dir: Vector2, dmg: float) -> void:
	damage = dmg
	velocity = dir.normalized() * speed
"

[sub_resource type="RectangleShape2D" id="rect"]
size = Vector2(26, 4)

[node name="player_projectile" type="Area2D"]
script = SubResource("GDScript_nyyeu")

[node name="Sprite2D" type="Sprite2D" parent="."]
scale = Vector2(0.05, 0.05)
texture = ExtResource("1_tex")

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
shape = SubResource("rect")
